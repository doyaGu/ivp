# Build-tree package configuration for IVP.
# Generated by configure_file() — paths are absolute to this build tree.

include(CMakeFindDependencyMacro)
include("${CMAKE_CURRENT_LIST_DIR}/../ivp_targets.cmake")

set(IVP_LIBRARIES
    ivp::ivp_physics
    ivp::ivp_collision
    ivp::ivp_controller
    ivp::ivp_compact_builder
    ivp::ivp_surface_manager
    ivp::ivp_utility
)

set(IVP_INCLUDE_DIRS "@PROJECT_BINARY_DIR@/include/ivp")
